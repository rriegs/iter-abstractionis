# iter-abstractionis

An incremental roguelike about finding what's left when all else is taken away.

## Getting Started

Open `index.html` in a modern web browser to play the current prototype. The
initial implementation demonstrates the first quest with simple progress bars
implemented in plain HTML, CSS and JavaScript.
